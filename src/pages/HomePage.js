import { Grid } from "@material-ui/core/";
import { } from "../components/";

export default function HomePage(props) {
	return (
		<Grid container>
			<Grid item xs={12}>
				<p> user </p>
			</Grid>
	
		</Grid>
	);
}
